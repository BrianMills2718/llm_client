# docs/meta-patterns

This directory holds the installed meta-process pattern docs consumed by
`llm_client`.

## Use This Directory For

- understanding planning, documentation-graph, and read-gating patterns as
  applied in this repo

## Do Not Use This Directory For

- repo-specific runtime boundaries that belong in root `CLAUDE.md`
- ADR decisions that belong in `../adr/`
