# docs

This subtree contains repo-local documentation for `llm_client`.

## Route By Question

- architecture decisions -> `adr/`
- active plans and plan status -> `plans/`
- installed meta-process pattern docs -> `meta-patterns/`
- notebook process artifacts -> `notebooks/`

## Working Rules

- Keep this parent at routing level.
- Put timeless runtime and package rules in the repo root or `llm_client/`, not
  in this parent docs file.
