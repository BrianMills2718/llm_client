# llm_client/prompts

This directory contains packaged prompt assets shipped with `llm_client`.

## Working Rules

- Keep prompts as data.
- Preserve the prompt-asset contract used by callers and prompt references.
- Do not embed shadow policy here that belongs in repo governance.
