# hooks

This directory contains the installed git hook entrypoints for `llm_client`.

## Working Rules

- Prefer changing canonical hook templates or generators before editing these
  installed files directly.
- Keep repo-local hook changes narrow and justified.
